{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "npm ci --legacy-peer-deps && echo !"
    },
    "deploy": {
      "startCommand": "node app.js",
      "healthcheckPath": "/",
      "healthcheckTimeout": 100,
      "restartPolicyType": "NEVER"
    }
  }